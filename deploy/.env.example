# 生成日期：2026-02-02（Codex）
#
# 复制为 deploy/.env 后再启动：
#   cp deploy/.env.example deploy/.env

# Web 入口端口（Nginx 容器对外暴露）
AFN_HTTP_PORT=80

# 必填：JWT 密钥（生产环境务必修改为随机强密码）
SECRET_KEY=please-change-me-to-a-long-random-secret

# 是否启用 WebUI 登录（部署前决定；修改后需重启）
AFN_AUTH_ENABLED=false

# 是否允许 WebUI 自助注册（仅在启用登录时生效；修改后需重启）
AFN_AUTH_ALLOW_REGISTRATION=true

# 可选：日志级别
LOGGING_LEVEL=INFO

# 可选：OpenAI Key（若使用 OpenAI 模型/嵌入）
OPENAI_API_KEY=

