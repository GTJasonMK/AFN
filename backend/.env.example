# 后端环境变量示例（开发环境）
# 生成日期：2026-01-24
# 生成者：Codex

# 环境标识（development / production）
ENVIRONMENT=development

# 是否开启调试
DEBUG=true

# JWT 密钥：开发环境可使用默认值；生产环境请务必替换为随机强密钥
SECRET_KEY=afn-desktop-secret-key-2024

# LLM 配置（按需填写）
# OPENAI_API_KEY=
# OPENAI_API_BASE_URL=
# OPENAI_MODEL_NAME=gpt-4o-mini

# 数据库配置（桌面版会由 run_app.py 注入 DATABASE_URL；独立运行后端可自行配置）
# DB_PROVIDER=sqlite
# DATABASE_URL=sqlite+aiosqlite:///./storage/afn.db

# WebUI 用户系统（可选）
# - true：启用登录认证与多用户数据隔离（按 user_id）
# - false：保持单用户模式（默认注入 desktop_user）
#
# 注意：桌面版（PyQt）默认不走登录流程，因此建议保持关闭；
# 如需启用 WebUI 用户系统，请在 backend/.env 中取消注释并设置为 true。
# AFN_AUTH_ENABLED=true

# 是否允许自助注册（仅在启用登录时生效）
# AFN_AUTH_ALLOW_REGISTRATION=true

# 初始管理员密码（仅在启用登录时使用）
# - 首次启用登录时建议显式配置，避免默认弱口令遗留；
# - 若未配置且为开发环境，后端将生成随机密码并在启动日志中输出一次；
# - 若为生产环境且未配置，后端会拒绝启动并提示补齐该变量。
# AFN_INITIAL_ADMIN_PASSWORD=please-change-me
